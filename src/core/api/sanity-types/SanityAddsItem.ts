export interface SanityAddsItem {
  mainAdd: {
    image: {
      alt: string;
      asset: {
        _ref: string;
      };
    };
  };
  boxAdds: {
    image: {
      alt: string;
      asset: {
        _ref: string;
      };
    };
  }[];
}
